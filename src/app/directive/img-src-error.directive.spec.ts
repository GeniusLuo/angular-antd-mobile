import { ImgSrcErrorDirective } from './img-src-error.directive';

describe('ImgSrcErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgSrcErrorDirective();
    expect(directive).toBeTruthy();
  });
});
